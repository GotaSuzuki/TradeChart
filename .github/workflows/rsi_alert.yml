name: RSI Alert Scheduler

on:
  schedule:
    - cron: '30 19 * * *' # JST 04:30 (UTC-9)
    - cron: '0 22 * * *'  # JST 07:00 (UTC-9)
  workflow_dispatch:

jobs:
  rsi-alert:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run RSI alert script
        env:
          CHANNEL_ACCESS_TOKEN: ${{ secrets.CHANNEL_ACCESS_TOKEN }}
          CHANNEL_SECRET: ${{ secrets.CHANNEL_SECRET }}
          LINE_TARGET_USER_ID: ${{ secrets.LINE_TARGET_USER_ID }}
          RSI_ALERT_THRESHOLD: ${{ secrets.RSI_ALERT_THRESHOLD }}
        run: |
          python scripts/rsi_alert.py NVDA AAPL
